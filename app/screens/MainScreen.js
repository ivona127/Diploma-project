import { View, Text } from 'react-native';

const MainScreen = () =>{
    return (
        <View>
            <Text>Welcome from 'MainScreen'</Text>
        </View>
    );
};

export default MainScreen;